name: sistem_dapur_pintar
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.10.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # --- DATABASE & IOT ---
  firebase_core: ^2.24.2
  firebase_database: ^10.4.0
  
  # --- UI & CHART ---
  fl_chart: ^0.66.0
  intl: ^0.19.0
  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0      # <--- WAJIB DITAMBAHKAN (Agar font Outfit jalan)
  
  # --- NOTIFIKASI & ALARM ---
  flutter_local_notifications: ^17.0.0
  audioplayers: ^5.2.1

  # --- LAPORAN & SUARA ROBOT ---
  pdf: ^3.10.4
  printing: ^5.11.0
  flutter_tts: ^3.8.3
  google_generative_ai: ^0.4.0
  provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true